// gets your app's root path
const root = '../';

module.exports = {
  index: async (req, res) => { await DefaultViewService.index(req, res, root); },
  create: async (req, res) => { await DefaultViewService.create(req, res, root); },
  edit: async (req, res) => { await DefaultViewService.edit(req, res, root); },
  show: async (req, res) => { await DefaultViewService.show(req, res, root); },
};

